# analytics-engine-c_car_platform

Python analytics engine container (placeholder).

## Local development

### Environment
Copy and edit:
- `.env.example` -> `.env`

### Tooling
This container uses:
- `black` for formatting
- `isort` for import sorting
- `flake8` for linting

### Commands

```bash
make install
make format
make lint
make test
```
